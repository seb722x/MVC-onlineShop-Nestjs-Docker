<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
    
    <link href="/css/app.css" rel="stylesheet" />
    <title>{{#if viewData.title}}{{ viewData.title }}{{else}}Online Store{{/if}}</title>


  </head>
  <body>
    

   <!-- header -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-secondary py-3">
        <div class="container">
            <a class="navbar-brand" href="#">Nepal Store</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav ms-auto">
                    <a class="nav-link active" href="/">Home</a>
                    <a class="nav-link active" href="/products">Products</a>
                    <a class="nav-link active" href="/about">About</a>
                    <a class="nav-link active" href="/cart">Cart</a>

                    <div class="vr bg-white mx-4 d-none d-lg-block"></div>
                    <a class="nav-link active" href="/cart/orders">My Orders</a>
                    <a class="nav-link active" href="/auth/register">Register</a>
                    {{#if session.user}}
                    <a class="nav-link active" href="/auth/logout">Logout ({{session.user.name}})</a>
                    {{else}}
                    <a class="nav-link active" href="/auth/login">Login</a>
                    <a class="nav-link active" href="/admin">Admin</a>
                    {{/if}}
                </div>
            </div>
        </div>
    </nav>

    <header class="masthead bg-primary text-white text-center py-5">
        <div class="container d-flex align-items-center flex-column">
            <h2>{{#if viewData.subtitle}}{{ viewData.subtitle }}{{else}}A Nest.js Online Store{{/if}}</h2>
        </div>
    </header>
    <!-- header -->

    <div class="container my-5">
    {{> content}}
    </div>


    <!-- footer -->
    <div class="copyright py-4 text-center text-white">
        <div class="container">
            <small>
                Nest online Store - Juan Sebastián
            </small>
        </div>
    </div>
    <!-- footer -->










    <!-- Option 1: Bootstrap Bundle with Popper -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous">
    </script>


  </body>
</html>